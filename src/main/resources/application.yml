spring:
  application:
    name: BankApp
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/blogdb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect