spring:
  application:
    name: BankApp
  datasource:
    password:
    url: jdbc:mysql://localhost:3306/blogdb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect